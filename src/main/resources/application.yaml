server:
  error:
    include-stacktrace: never
spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/city_management-db?createDatabaseIfNotExist=true
    username: root
    password: password

